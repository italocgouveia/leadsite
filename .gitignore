# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# o exemplo NAO tem segredo e precisa ir junto: e o mapa das variaveis

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# rascunhos locais que nao fazem parte do projeto
id.txt
_teste_*.json
_teste_*.ts

# scripts de supervisor local (Task Scheduler/servico.bat/.vbs) — caminhos e
# nvm4w hardcoded desta maquina, sem serventia no deploy da Vercel. O
# equivalente da bridge (whatsapp-node) tambem nao entra em nenhum git.
atualizar.bat
desinstalar-servico.bat
iniciar-painel.bat
instalar-servico.bat
servico.bat
servico.vbs

# O exemplo NAO tem segredo e precisa ir junto: e o mapa das variaveis.
# Fica no fim de proposito — como havia dois `.env*` no arquivo, a excecao
# posta no meio era anulada pelo padrao que vinha depois.
!.env.example
painel.log
*.log
geracao.pausado

# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# o exemplo NAO tem segredo e precisa ir junto: e o mapa das variaveis

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# rascunhos locais que nao fazem parte do projeto
id.txt

# O exemplo NAO tem segredo e precisa ir junto: e o mapa das variaveis.
# Fica no fim de proposito — como havia dois `.env*` no arquivo, a excecao
# posta no meio era anulada pelo padrao que vinha depois.
!.env.example
